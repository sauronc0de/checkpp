{
  "name": "🐳[${localWorkspaceFolderBasename}]",
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "WORKSPACE_DIR": "/workspaces/${localWorkspaceFolderBasename}",
      "PROJECT_NAME": "${localWorkspaceFolderBasename}"
    }
  },
  "containerEnv": {
    "ENGRAM_DATA_DIR": "${containerWorkspaceFolder}/.engram" // Path where the Engram database is stored. Should be located inside the workspace folder so data persists across container rebuilds.
  },
  "mounts": [
    "source=${localEnv:HOME}/.local/share/opencode,target=/opt/opencode-host,type=bind"
  ],
  "postStartCommand": "sudo chown -R $(id -u):$(id -g) /workspaces/${localWorkspaceFolderBasename} && chmod +x .devcontainer/docker_entrypoint.sh && .devcontainer/docker_entrypoint.sh",
  "customizations": {
    "vscode": {
      "extensions": [
        "mhutchie.git-graph",
        "ms-vscode.cmake-tools",
        "ms-azuretools.vscode-docker",
        "aaron-bond.better-comments",
        "seunlanlege.action-buttons",
        "ms-vscode.makefile-tools",
        "vadimcn.vscode-lldb",
        "ms-vscode.cpptools",
        "ms-vscode.cpptools-extension-pack",
        "jebbs.plantuml",
        "bierner.markdown-mermaid",
        "slevesque.shader"
      ]
    }
  }
}